<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PRM392"
        tools:targetApi="31">



            <activity
                android:name=".activity.Admin.ProductList"
                android:exported="true">
                <intent-filter>
                    <action android:name="android.intent.action.MAIN" />
                    <category android:name="android.intent.category.LAUNCHER" />
                </intent-filter>
            </activity>

        <activity
            android:name=".activity.Admin.DetailActivity"
            android:exported="true" />

        <activity
            android:name=".activity.Admin.UpdateActivity"
            android:exported="true" />



        <activity
            android:name=".activity.User.OTPVerificationActivity"
            android:exported="true" />

        <activity
            android:name=".activity.Admin.UploadActivity"
            android:exported="true" />


    </application>
</manifest>
